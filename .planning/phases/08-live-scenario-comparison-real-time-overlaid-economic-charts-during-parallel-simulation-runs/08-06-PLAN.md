---
phase: 08-live-scenario-comparison
plan: 06
type: execute
wave: 3
depends_on: [08-01, 08-03]
files_modified:
  - web/src/pages/HomePage.tsx
  - web/src/pages/Reflection.tsx
  - web/src/stores/reflectionStore.ts
  - web/src/pages/AgentReview.tsx
  - web/src/pages/Artifacts.tsx
  - server/src/llm/prompts/comparison.ts
  - server/src/routes/reflect.ts
autonomous: true
requirements: [LSC-05, LSC-06, LSC-07, LSC-08]
must_haves:
  truths:
    - "Home page shows grouped sessions as a single card with scenario count badge"
    - "Clicking grouped card navigates to the base session's current stage"
    - "Reflection page shows side-by-side evaluations with cross-scenario data table and LLM narrative"
    - "Agent reflections show per-agent comparison cards across all scenarios"
    - "Review page agent chat includes context from all scenarios for cross-scenario questions"
    - "Artifacts page produces per-scenario exports plus a combined markdown policy brief"
  artifacts:
    - path: "web/src/pages/HomePage.tsx"
      provides: "Grouped session cards with badge"
      contains: "scenarios"
    - path: "web/src/pages/Reflection.tsx"
      provides: "Multi-scenario side-by-side evaluations"
      contains: "cross-scenario"
      min_lines: 200
    - path: "web/src/pages/AgentReview.tsx"
      provides: "Multi-scenario context in agent chat"
      contains: "scenario"
    - path: "web/src/pages/Artifacts.tsx"
      provides: "Per-scenario exports + combined policy brief"
      contains: "policy brief"
  key_links:
    - from: "web/src/pages/HomePage.tsx"
      to: "server/src/routes/sessions.ts"
      via: "sessions list with groupId"
      pattern: "groupId"
    - from: "web/src/pages/Reflection.tsx"
      to: "web/src/stores/reflectionStore.ts"
      via: "multi-session reflection state"
      pattern: "reflectionStore"
---

<objective>
Implement session grouping on Home page and multi-scenario adaptations for Reflection, Review, and Artifacts pages.

Purpose: Completes the end-to-end multi-scenario experience. Home page groups scenario sessions into single cards per D-35/D-37. Reflection shows side-by-side evaluations per D-43/D-45. AgentReview includes cross-scenario context per D-48. Artifacts generates combined policy brief per D-49.

Output: Home page grouped cards, multi-scenario Reflection/Review/Artifacts pages, cross-scenario LLM prompt, policy brief generation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-live-scenario-comparison-real-time-overlaid-economic-charts-during-parallel-simulation-runs/08-CONTEXT.md
@.planning/phases/08-live-scenario-comparison-real-time-overlaid-economic-charts-during-parallel-simulation-runs/08-RESEARCH.md
@.planning/phases/08-live-scenario-comparison-real-time-overlaid-economic-charts-during-parallel-simulation-runs/08-UI-SPEC.md
@.planning/phases/08-live-scenario-comparison-real-time-overlaid-economic-charts-during-parallel-simulation-runs/08-01-SUMMARY.md

<interfaces>
From shared/src/types.ts:
```typescript
export interface SessionMetadata {
  id: string; title: string; stage: Stage;
  createdAt: string; updatedAt: string; completedAt: string | null;
  agentCount: number;
  groupId: string | null;       // Added Plan 01
  scenarioLabel: string | null; // Added Plan 01
}

export interface SocietyEvaluation {
  summary: string;
  metrics?: Record<string, number>;
}

export interface AgentReflection {
  agentId: string;
  agentName: string;
  role: string;
  reflection: string;
}
```

From server/src/llm/prompts/comparison.ts:
```typescript
// Comparison prompt for 8-dimension scoring
// Builds prompt with telemetry data from 2 sessions
```

From web/src/pages/HomePage.tsx:
```typescript
// Renders session list from API, each as a glass card
// Clicking navigates to session's current stage
```

From web/src/pages/Reflection.tsx:
```typescript
// 2-panel layout: Society Evaluation | Agent Reflections
// Fetches reflections via API, triggers LLM evaluation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home page grouped session cards and Reflection multi-scenario layout</name>
  <files>web/src/pages/HomePage.tsx, web/src/pages/Reflection.tsx, web/src/stores/reflectionStore.ts</files>
  <read_first>web/src/pages/HomePage.tsx, web/src/pages/Reflection.tsx, web/src/stores/reflectionStore.ts, server/src/routes/sessions.ts</read_first>
  <action>
**Home Page (D-37, D-38, LSC-05 UI):**

1. In `web/src/pages/HomePage.tsx`, modify the session list rendering:
   - After fetching sessions, group by `groupId` (sessions with null groupId are standalone)
   - For each group: render a single card showing:
     - Title from the base session (first in group by createdAt)
     - Stage from base session (D-38)
     - Badge: `<span style={{ display: 'inline-flex', padding: '2px 8px', borderRadius: 12, background: 'var(--primary-alpha, rgba(79,70,229,0.1))', color: 'var(--primary)', fontSize: 12, fontWeight: 700 }}>{count} scenarios</span>`
     - Comma-separated scenario labels below in `var(--text-dim)` at 12px
   - Click navigates to base session's current stage
   - Standalone sessions (null groupId) render as before with no badge
   - Grouping logic:
     ```typescript
     const grouped = new Map<string, SessionMetadata[]>();
     const standalone: SessionMetadata[] = [];
     for (const s of sessions) {
       if (s.groupId) {
         const g = grouped.get(s.groupId) ?? [];
         g.push(s);
         grouped.set(s.groupId, g);
       } else {
         standalone.push(s);
       }
     }
     ```

**Reflection Page (D-43, D-44, D-45, LSC-06):**

2. In `web/src/pages/Reflection.tsx`, add multi-scenario support:
   - Read `?scenarios=id1,id2` from URL (same pattern as Simulation page)
   - If N>1, fetch reflections for ALL scenario sessions
   - **Cross-Scenario Summary** (top section):
     - Data comparison table: key metrics (final CPI, final M1, avg wealth, avg happiness, population alive) per scenario in columns
     - Fetch telemetry/stats from each session to populate
     - Below table: LLM-generated narrative interpreting differences. Call a new endpoint or generate client-side using existing comparison prompt pattern. Use the `POST /api/compare` endpoint pattern from comparison.ts to get an LLM narrative.
     - Alternatively: fetch each session's `societyEvaluation` and display side-by-side, then generate a cross-scenario narrative via a new `POST /api/reflect/cross-scenario` endpoint
   - **Agent Reflections** (bottom section per D-45):
     - Group agents by name across scenarios
     - For each agent: glass card with scenario-colored left borders (4px solid scenario color)
     - Each card shows the agent's reflection from each scenario stacked vertically:
       ```
       Agent: Maria (Farmer)
       ├ [blue border] Baseline: "I struggled with..."
       ├ [orange border] Policy A: "The higher taxes..."
       └ [green border] Policy B: "Without spending..."
       ```
   - For N=1: render exactly as current (no cross-scenario section)

3. In `web/src/stores/reflectionStore.ts`, extend to support multi-session:
   - Add `scenarioReflections: Record<string, { evaluation: SocietyEvaluation | null; agentReflections: AgentReflection[] }>` keyed by sessionId
   - Add `loadScenarioReflections(sessionIds: string[])` that fetches for all scenarios
   - Add `crossScenarioNarrative: string | null` for the LLM interpretation
  </action>
  <verify>
    <automated>cd C:/Users/16079/Code/PolicyLab && npx tsc --noEmit -p web/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - web/src/pages/HomePage.tsx groups sessions by `groupId`
    - web/src/pages/HomePage.tsx contains "scenarios" badge text
    - web/src/pages/HomePage.tsx standalone sessions (null groupId) render without badge
    - web/src/pages/Reflection.tsx reads `scenarios` from URL search params
    - web/src/pages/Reflection.tsx contains cross-scenario data table rendering
    - web/src/pages/Reflection.tsx contains agent comparison cards with scenario-colored borders
    - web/src/stores/reflectionStore.ts contains `scenarioReflections` state
    - TypeScript compiles
  </acceptance_criteria>
  <done>Home page groups scenario sessions into single cards with badges. Reflection shows side-by-side evaluations, cross-scenario data table, and per-agent comparison cards across scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: AgentReview cross-scenario context and Artifacts combined policy brief</name>
  <files>web/src/pages/AgentReview.tsx, web/src/pages/Artifacts.tsx, server/src/llm/prompts/comparison.ts, server/src/routes/reflect.ts</files>
  <read_first>web/src/pages/AgentReview.tsx, web/src/pages/Artifacts.tsx, server/src/llm/prompts/comparison.ts, server/src/routes/reflect.ts</read_first>
  <action>
**AgentReview (D-48, LSC-07):**

1. In `web/src/pages/AgentReview.tsx`, modify to include cross-scenario context:
   - Read `?scenarios=id1,id2` from URL
   - If N>1, when building the agent chat context/system prompt, include reflections and stats from ALL scenarios:
     ```
     You are reviewing {agentName} who lived through {N} different policy scenarios.
     
     In "{scenarioLabel1}" (Baseline): {reflection1}. Final stats: wealth={w1}, health={h1}, happiness={hap1}.
     In "{scenarioLabel2}" (Policy A): {reflection2}. Final stats: wealth={w2}, health={h2}, happiness={hap2}.
     
     The user may ask cross-scenario comparison questions like "How did you fare under Policy A vs Baseline?"
     Answer based on the lived experience in each scenario.
     ```
   - Fetch agent data and reflections from all scenario sessions
   - The chat UI itself stays as a single chat per agent (D-48) — just the context is enriched
   - For N=1: no change to existing behavior

**Artifacts (D-49, LSC-08):**

2. In `web/src/pages/Artifacts.tsx`, add multi-scenario support:
   - Read `?scenarios=id1,id2` from URL
   - **Per-scenario exports**: render existing export functionality for each scenario session (download JSON export per session)
   - **Combined policy brief**: when N>1, add a "Combined Policy Brief" section:
     - Call `POST /api/reflect/policy-brief` with all scenario session IDs
     - Server generates a markdown policy brief containing:
       - Executive summary
       - Scenario comparison table (parameters that differ, key outcome metrics)
       - Key findings per scenario
       - Agent outcome highlights (how representative agents fared differently)
       - Policy recommendations
     - Render markdown using existing `MarkdownText` component
     - "Download Policy Brief" button to download as .md file
   - UI-SPEC copy: title "Combined Policy Brief", empty state "Policy brief will be generated after all scenarios complete."

3. In `server/src/llm/prompts/comparison.ts` (or create `server/src/routes/policyBrief.ts`):
   - Add `buildPolicyBriefPrompt(scenarios: Array<{ label: string; evaluation: string; telemetry: TelemetryLog[]; configDiffs: Record<string, { baseline: number; scenario: number }> }>): string`
   - The prompt asks the LLM to generate a markdown policy brief with the sections listed above
   - Mount as `POST /api/reflect/policy-brief` accepting `{ sessionIds: string[] }`
   - Fetch evaluations and telemetry for all sessions, build prompt, call LLM, return markdown

4. In `server/src/routes/reflect.ts` (or wherever reflections are routed), add the cross-scenario endpoints:
   - `POST /api/reflect/cross-scenario` — generates LLM narrative comparing scenario outcomes
   - `POST /api/reflect/policy-brief` — generates combined policy brief markdown
  </action>
  <verify>
    <automated>cd C:/Users/16079/Code/PolicyLab && npx tsc --noEmit -p web/tsconfig.json && npx tsc --noEmit -p server/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - web/src/pages/AgentReview.tsx reads `scenarios` from URL search params
    - web/src/pages/AgentReview.tsx includes multi-scenario agent context in chat
    - web/src/pages/Artifacts.tsx contains "Combined Policy Brief" text
    - web/src/pages/Artifacts.tsx contains per-scenario export rendering
    - web/src/pages/Artifacts.tsx contains "Download Policy Brief" button or link
    - Server has a policy brief generation endpoint
    - server/src/llm/prompts/comparison.ts contains `buildPolicyBriefPrompt` or similar
    - TypeScript compiles for both web and server
  </acceptance_criteria>
  <done>AgentReview chat includes cross-scenario context for multi-scenario questions. Artifacts page shows per-scenario exports plus combined LLM-generated policy brief with download.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p web/tsconfig.json` compiles
- `npx tsc --noEmit -p server/tsconfig.json` compiles
- `npm run test -w server -- --run` existing tests pass
</verification>

<success_criteria>
- Home page groups scenario sessions with count badge and navigates to base session stage
- Reflection shows cross-scenario data table, LLM narrative, and per-agent comparison cards
- AgentReview chat has multi-scenario context for cross-scenario questions
- Artifacts generates and displays combined policy brief with download option
- All pages work normally for N=1 (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/08-live-scenario-comparison-real-time-overlaid-economic-charts-during-parallel-simulation-runs/08-06-SUMMARY.md`
</output>
