---
phase: 10-fix-simulation-realism
plan: 08
type: execute
wave: 5
depends_on: ["10-06", "10-07"]
files_modified:
  - server/src/orchestration/simulationRunner.ts
  - server/src/routes/bootstrap.ts
autonomous: false
requirements:
  - D-08
  - D-11
  - D-22
  - D-27
must_haves:
  truths:
    - "Full simulation run with location-bootstrapped session shows enterprise wages, AMM trades, and banking activity"
    - "Narrative grounding produces prose with embedded numbers matching telemetry"
    - "Agent reflections reference personal stat trajectory"
    - "Bootstrap fails cleanly when LLM enrichment errors occur"
    - "All existing tests still pass with no regressions"
  artifacts:
    - path: "server/src/orchestration/simulationRunner.ts"
      provides: "Fully integrated enterprise + inflation + narrative loop"
  key_links:
    - from: "server/src/orchestration/simulationRunner.ts"
      to: "server/src/mechanics/enterpriseEngine.ts"
      via: "enterprise processing in iteration loop"
      pattern: "processEnterpriseWages"
    - from: "server/src/orchestration/simulationRunner.ts"
      to: "server/src/llm/narrativeValidation.ts"
      via: "validation after narrative generation"
      pattern: "validateNarrative"
---

<objective>
Integration verification: ensure all Phase 10 subsystems work together in a real simulation run, fix any wiring issues, and human-verify the end-to-end experience.

Purpose: Plans 02-07 built individual subsystems (enterprise engine, bootstrap, banking, inflation, narrative, prompts). This plan ensures they're correctly integrated: enterprises produce goods, agents get paid, banking flows through deposits, CPI responds to price changes, narratives match data, and reflections use real trajectories.

Output: Integration fixes applied. Human verification of a complete simulation run.

Note: Wave bumped from 4 to 5 due to cascading wave adjustment (Plan 05 moved to Wave 3, Plan 06 moved to Wave 4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-fix-simulation-realism-agent-economic-behavior-inflation-response-narrative-grounding/10-CONTEXT.md
@server/src/orchestration/simulationRunner.ts
@server/src/routes/bootstrap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration smoke test + fix wiring issues</name>
  <files>server/src/orchestration/simulationRunner.ts, server/src/routes/bootstrap.ts</files>
  <read_first>server/src/orchestration/simulationRunner.ts, server/src/routes/bootstrap.ts, server/src/mechanics/enterpriseEngine.ts, server/src/llm/narrativeValidation.ts, server/src/mechanics/inflationEngine.ts</read_first>
  <action>
Run the full test suite and fix any issues:

1. `npm run test -w server -- --run` -- fix any failures
2. `npx tsc -p server/tsconfig.json --noEmit` -- fix any type errors
3. `npx tsc -p shared/tsconfig.json --noEmit` -- fix any shared type errors

Review the integration points in simulationRunner.ts:

- Verify enterprise engine imports and calls are syntactically correct
- Verify Taylor Rule is called within the inflation block when centralBankEnabled
- Verify narrative validation is called after resolution LLM and re-generation works
- Verify agent stat trajectory is queried and passed to reflection prompt
- Verify enterprise deposit accounts are created at simulation start
- Verify SFC audit baseline accounts for liquidity injection

Check bootstrap.ts:
- Verify generateEnterprises is called after generateAgentRoster
- Verify enterprise blueprints are persisted to DB via enterpriseRepo
- Verify bootstrap failure on LLM enrichment error sends SSE error event

Fix any TypeScript compile errors, missing imports, incorrect parameter passing, or runtime connection issues found during review.

Also verify the QUIT_JOB action handler (D-08) is wired correctly. It already exists in simulationRunner.ts (lines 704-716) and handles: removing agent from employmentRegistry, removing from enterprise.employees set, clearing state.employer_id. Verify this handler works correctly with the new enterprise engine from Plan 02 -- specifically that the enterpriseRegistry lookup at line 709 still resolves after enterprise engine changes.
  </action>
  <verify>
    <automated>cd C:/Users/16079/Code/PolicyLab && npx tsc -p server/tsconfig.json --noEmit 2>&1 | head -5 && npm run test -w server -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compiles with zero errors for both server and shared
    - All server tests pass (including new Phase 10 tests)
    - simulationRunner.ts QUIT_JOB handler verified working with enterprise engine
    - bootstrap.ts calls generateEnterprises
    - No console.error or unhandled promise rejections in test output
  </acceptance_criteria>
  <done>All integration points verified. TypeScript clean. Full test suite green. QUIT_JOB verified. Bootstrap generates enterprises.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of end-to-end simulation realism</name>
  <action>Human verifies that a full simulation run with location-bootstrapped session shows realistic economic behavior: enterprises functioning, banking active, narratives grounded in data, reflections personal.</action>
  <what-built>
    Complete Phase 10 simulation realism overhaul:
    - Enterprise auto-creation from bootstrap data (2-3 enterprises per sector)
    - Enterprise wage system with minimum wage enforcement
    - Enterprise insolvency tracking (3-iteration bankruptcy)
    - Idle-agent subsistence fallback (5 food after 2 idle iterations)
    - Differentiated loan products (business vs personal)
    - Taylor Rule central bank with rate ceiling and reserve escalation
    - Data-driven narrative grounding with telemetry digest
    - Narrative validation with re-generation on contradiction
    - Agent reflection stat trajectory
    - Banking action ROI-focused prompts
  </what-built>
  <how-to-verify>
    1. Start the app: `npm run dev`
    2. Create a new location-mode session:
       - Go to http://localhost:5173
       - Choose "Mirror a Real Location"
       - Search for "China" or any country
       - Set 30 agents
       - Add scenario: "Minimum wage increased by 30%"
       - Click Bootstrap
    3. Verify bootstrap creates enterprises:
       - In Design Review, check agent roster -- agents should have enterprise assignments
       - Economy tab should show minimumWage parameter
    4. Start simulation (10 iterations)
    5. During simulation, observe:
       - Narrative text should include specific numbers (food price, wealth changes)
       - Narrative tone should match data direction (not always negative)
       - Agent actions should include WORK_AT_ENTERPRISE, DEPOSIT, TAKE_LOAN
    6. After simulation, go to Reflection:
       - Agent reflections should reference their personal wealth/health trajectory
    7. Check console output for:
       - No SFC audit failures
       - Taylor Rule traces in logs
       - No "[ASSERTION]" warnings about agent data injection
  </how-to-verify>
  <verify>Manual human verification</verify>
  <acceptance_criteria>
    - Enterprises visible in Design Review agent roster
    - Simulation narratives contain embedded numbers from telemetry
    - Agent actions include enterprise and banking actions
    - No SFC audit failures in console
    - Agent reflections reference personal stat trajectory
  </acceptance_criteria>
  <resume-signal>Type "approved" if simulation shows realistic economic behavior, enterprises functioning, and narratives grounded in data. Describe any issues if not.</resume-signal>
  <done>Human confirms simulation produces economically coherent outcomes with active enterprises, grounded narratives, and personal reflections.</done>
</task>

</tasks>

<verification>
- TypeScript compiles clean for all workspaces
- Full test suite green
- Human verification of end-to-end simulation behavior
</verification>

<success_criteria>
All Phase 10 subsystems work together in a real simulation. Enterprises produce goods and pay wages. Banking is actively used. CPI responds to price shocks. Narratives match telemetry data. Agent reflections reference personal journeys. No SFC violations. Human approval of simulation realism.
</success_criteria>

<output>
After completion, create `.planning/phases/10-fix-simulation-realism-agent-economic-behavior-inflation-response-narrative-grounding/10-08-SUMMARY.md`
</output>
