---
phase: 07-real-world-scenario-bootstrap
plan: 05
type: execute
wave: 4
depends_on: ["07-03", "07-04"]
files_modified:
  - web/src/pages/IdeaInput.tsx
  - web/src/pages/DesignReview.tsx
  - server/src/routes/bootstrap.ts
autonomous: false

requirements:
  - RWB-01
  - RWB-02
  - RWB-03
  - RWB-04
  - RWB-05
  - RWB-06
  - RWB-07

must_haves:
  truths:
    - "End-to-end flow works: select Mirror a Real Location -> search location -> set agent count -> begin bootstrap -> progress panel updates -> arrive at Design Review with generated session"
    - "Economy tab in Design Review shows auto-generated EconomyConfig with confidence badges"
    - "Scenario tabs allow creating a variant and showing diff markers"
    - "Creative brainstorm flow still works end to end"
  artifacts:
    - path: "web/src/pages/IdeaInput.tsx"
      provides: "Final polished dual-mode IdeaInput"
    - path: "web/src/pages/DesignReview.tsx"
      provides: "Final DesignReview with scenario tabs and diff markers"
  key_links:
    - from: "web/src/pages/IdeaInput.tsx"
      to: "server/src/routes/bootstrap.ts"
      via: "SSE connection for bootstrap pipeline"
      pattern: "bootstrap"
    - from: "web/src/pages/DesignReview.tsx"
      to: "web/src/components/ScenarioTabs.tsx"
      via: "renders scenario builder for location sessions"
      pattern: "ScenarioTabs"
---

<objective>
End-to-end integration verification and polish. Fix any wiring issues between the frontend (Plans 03-04) and backend (Plan 02). Ensure the full flow works: location selection -> bootstrap -> Design Review with scenarios.

Purpose: Integration testing and final polish before the phase is considered complete.

Output: Bug fixes, wiring corrections, verified end-to-end flow
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-real-world-scenario-bootstrap-location-based-data-driven-society-design-for-policymakers/07-RESEARCH.md
@.planning/phases/07-real-world-scenario-bootstrap-location-based-data-driven-society-design-for-policymakers/07-03-SUMMARY.md
@.planning/phases/07-real-world-scenario-bootstrap-location-based-data-driven-society-design-for-policymakers/07-04-SUMMARY.md

@web/src/pages/IdeaInput.tsx
@web/src/pages/DesignReview.tsx
@server/src/routes/bootstrap.ts
@web/src/stores/bootstrapStore.ts
@web/src/stores/scenarioStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end integration wiring and fix</name>
  <files>
    web/src/pages/IdeaInput.tsx,
    web/src/pages/DesignReview.tsx,
    server/src/routes/bootstrap.ts
  </files>
  <read_first>
    web/src/pages/IdeaInput.tsx,
    web/src/pages/DesignReview.tsx,
    server/src/routes/bootstrap.ts,
    web/src/stores/bootstrapStore.ts,
    web/src/stores/scenarioStore.ts,
    web/src/components/ScenarioTabs.tsx,
    web/src/components/LocationSearch.tsx,
    server/src/data/locationDataService.ts,
    server/src/data/dataBootstrapPipeline.ts,
    server/src/routes/index.ts
  </read_first>
  <action>
    Verify and fix the end-to-end integration:

    **1. Build check:** Run `npm run build` and fix any type errors or missing imports.

    **2. Server route verification:**
    - Ensure bootstrap.ts properly creates a session in 'idea-input' stage or accepts an existing session.
    - Ensure the SSE response correctly terminates (`res.end()`) after the complete event.
    - Ensure the location search endpoint returns the correct shape for LocationSearch component.

    **3. Frontend wiring verification:**
    - IdeaInput: Verify bootstrapStore.startBootstrap correctly creates a session THEN opens SSE connection.
    - IdeaInput: Verify navigation to `/session/${id}/design-review` uses the correct session ID from the bootstrap completion.
    - DesignReview: Verify that when a location-bootstrapped session loads, the session.config contains locationProfile and the ScenarioTabs are shown on the Economy tab.
    - DesignReview: Verify that the scenarioStore.initFromSession is called with the correct config.

    **4. SSE parsing:**
    - Ensure the bootstrapStore SSE reader correctly handles the `data: {...}\n\n` format.
    - Handle edge case: multi-line SSE data, connection drops with retry.

    **5. Run all tests:**
    - `npm run test -w server -- --run` — all server tests pass.
    - `npm run build` — full build succeeds.
    - `npm run lint -w web` — no lint errors.

    **6. Fix any issues found.** Common issues to look for:
    - Import paths (`.js` extensions required for server ESM imports).
    - Missing route registration in index.ts.
    - Type mismatches between frontend expectations and server responses.
    - SSE event format mismatches.
  </action>
  <verify>
    <automated>npm run build && npm run test -w server -- --run && npm run lint -w web</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds with zero errors
    - `npm run test -w server -- --run` passes all tests
    - `npm run lint -w web` passes
    - server/src/routes/bootstrap.ts sends `res.end()` after complete event
    - Bootstrap route is accessible at POST /api/sessions/:id/bootstrap
    - Location search is accessible at GET /api/locations/search
  </acceptance_criteria>
  <done>Full monorepo builds, all tests pass, lint clean. Bootstrap route and location search are accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of end-to-end data-driven design flow</name>
  <files>web/src/pages/IdeaInput.tsx, web/src/pages/DesignReview.tsx</files>
  <action>
    Human verifies the complete "Mirror a Real Location" flow end-to-end, plus confirms existing creative brainstorm flow is preserved.
  </action>
  <verify>Human visual and functional verification</verify>
  <done>Both design flows work correctly: creative brainstorm is unchanged, location-based bootstrap produces a reality-calibrated session with scenario builder.</done>
  <what-built>
    Complete "Mirror a Real Location" data-driven design flow:
    1. Dual-mode IdeaInput page (creative vs. location-based)
    2. Location search with Photon autocomplete
    3. Bootstrap pipeline fetching real-world data from World Bank API
    4. Step-by-step progress panel during data fetch
    5. Auto-generated EconomyConfig, agent roster, and law document
    6. Scenario builder with tab-based multi-scenario management
    7. Inline diff markers and data confidence badges
    8. "Run All Scenarios" parallel execution
  </what-built>
  <how-to-verify>
    1. Start the app: `npm run dev`
    2. Go to http://localhost:5173
    3. Verify you see two cards: "Describe a Society" and "Mirror a Real Location"
    4. Click "Mirror a Real Location"
    5. Type "Brazil" in the location search — verify autocomplete shows results
    6. Select "Brazil" — verify country code and coordinates appear
    7. Set agent count to 30
    8. Optionally enter a scenario: "What if we double education spending?"
    9. Click "Begin Bootstrap" — verify step-by-step progress panel shows
    10. Wait for completion — verify you land on Design Review
    11. Click the Economy tab — verify auto-generated parameters with confidence badges
    12. Verify scenario tabs are shown (Baseline tab)
    13. Click "+ Add Scenario" — verify new tab clones baseline
    14. Change a parameter in Scenario A — verify diff marker appears
    15. Go back to home and verify "Describe a Society" still works normally (click, type idea, begin brainstorm)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Full build + tests + lint pass
- Human verification of end-to-end flow
- Both creative and location-based flows work
</verification>

<success_criteria>
- Policymaker can enter a real-world location and get a simulation starting point calibrated to reality
- Auto-generated EconomyConfig uses real economic data with correct unit conversions
- Agent roster reflects real demographic distribution with Gini-distributed wealth
- Scenario builder enables A/B policy comparison before simulation
- Existing creative brainstorm flow is fully preserved
</success_criteria>

<output>
After completion, create `.planning/phases/07-real-world-scenario-bootstrap-location-based-data-driven-society-design-for-policymakers/07-05-SUMMARY.md`
</output>
