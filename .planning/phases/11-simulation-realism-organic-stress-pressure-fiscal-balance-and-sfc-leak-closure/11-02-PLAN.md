---
phase: 11-simulation-realism-organic-stress-pressure-fiscal-balance-and-sfc-leak-closure
plan: 02
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - server/src/mechanics/physicsEngine.ts
  - server/src/orchestration/simulationRunner.ts
  - server/src/mechanics/__tests__/physicsEngine.cortisolStrip.test.ts
  - server/src/mechanics/__tests__/physicsEngine.happinessStrip.test.ts
  - server/src/mechanics/__tests__/structuralPressures.test.ts
  - server/src/orchestration/__tests__/statClamping.test.ts
autonomous: true
decisions_addressed:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-06
  - D-07
  - D-08

must_haves:
  truths:
    - "WORK, WORK_AT_ENTERPRISE, REST, PRODUCE_AND_SELL, POST_BUY_ORDER, POST_SELL_ORDER, HELP, DEPOSIT, REPAY_LOAN, BUY_BOND, ISSUE_GOV_BOND have cortisolDelta === 0"
    - "REST, POST_BUY_ORDER, POST_SELL_ORDER, PRODUCE_AND_SELL, DEPOSIT, REPAY_LOAN, BUY_SHARES, BUY_BOND have happinessDelta === 0"
    - "QUIT_JOB cortisolDelta === 4 and happinessDelta === -1 (outcome-driven, retained)"
    - "STEAL cortisolDelta === 10, STRIKE cortisolDelta === 5 (outcome-driven, retained)"
    - "HELP happinessDelta === 5 (altruistic outcome, retained) even though cortisol stripped"
    - "After structural pressure injection, bank agents and central_bank agents receive ZERO cortisol/happiness deltas from the pressure loop"
    - "Cortisol clamped to [3, 95] and happiness clamped to [5, 95] at final stat commit"
    - "SFC invariant unchanged: all existing SFC tests still green"
  artifacts:
    - path: server/src/mechanics/physicsEngine.ts
      provides: stripped per-action cortisol/happiness deltas
      contains: "per-action relief removed"
    - path: server/src/orchestration/simulationRunner.ts
      provides: structural pressure injection loop applied after inflation tick
      contains: "k_inflation_cor"
  key_links:
    - from: server/src/orchestration/simulationRunner.ts
      to: server/src/mechanics/physicsConfig.ts
      via: "physicsConfig.k_* coefficients consumed in structural pressure loop"
      pattern: "physicsConfig\\.k_inflation_cor|physicsConfig\\.k_gini_cor"
    - from: server/src/orchestration/simulationRunner.ts
      to: "agent filter"
      via: "bank/central exclusion before pressure application"
      pattern: "type\\s*!==\\s*'bank'"
---

<objective>
Strip per-action cortisol/happiness relief and per-action happiness rewards in physicsEngine.ts per D-01 and D-06. Add a structural pressure injection loop in simulationRunner.ts that applies 4 cortisol pressures and 5 happiness pressures to every alive citizen agent (excluding bank/central) each iteration per D-02 and D-07. Apply raised clamps [3, 95] (cortisol) and [5, 95] (happiness) at final stat commit per D-03 and D-08.

Purpose: Eliminate utopia bias. Cortisol stops decaying from routine actions; happiness stops saturating from routine actions. Both become signals shaped by real macro conditions (inflation, inequality, unemployment, public goods quality, peer deaths) rather than by agent activity level.
Output: 11 stripped action cases in physicsEngine; 1 new structural pressure block in simulationRunner; updated clamp bounds at stat commit sites; real assertions in 4 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-simulation-realism-organic-stress-pressure-fiscal-balance-and-sfc-leak-closure/11-CONTEXT.md
@.planning/phases/11-simulation-realism-organic-stress-pressure-fiscal-balance-and-sfc-leak-closure/11-RESEARCH.md
@.planning/phases/11-simulation-realism-organic-stress-pressure-fiscal-balance-and-sfc-leak-closure/11-01-SUMMARY.md
@CLAUDE.md
@server/src/mechanics/physicsEngine.ts
@server/src/mechanics/physicsConfig.ts
@server/src/orchestration/simulationRunner.ts

<interfaces>
<!-- Key existing signatures this plan consumes. Wave 0 landed these. -->

From `server/src/mechanics/physicsConfig.ts` (Wave 0 extended):
- `physicsConfig.k_inflation_cor` (default 0.3)
- `physicsConfig.k_gini_cor` (default 8.0)
- `physicsConfig.k_unemp_cor` (default 3.0)
- `physicsConfig.k_pg_cor` (default 2.0)
- `physicsConfig.k_peer_death_hap` (default 2.0)
- `physicsConfig.k_gini_hap` (default 6.0)
- `physicsConfig.k_unemp_hap` (default 2.5)
- `physicsConfig.k_welfare_hap` (default 2.5)
- `physicsConfig.k_inflation_hap` (default 0.25)
- `physicsConfig.giniStressThreshold` (default 0.35)
- `physicsConfig.cortisolCeiling` (95), `cortisolFloor` (3)
- `physicsConfig.happinessCeiling` (95), `happinessFloor` (5)

Existing runner state:
- `weekStateMap: Map<string, WeekState>` where WeekState has `cortisolDelta`, `happinessDelta`, `wealthDelta` fields
- `employmentRegistry: Set<string>` — agents with employment
- `aliveAgents: Agent[]` — all alive, includes bank/central_bank (must be filtered)
- `giniCoefficient: number` — already computed at `simulationRunner.ts:2566`
- `inflationOutput: { inflationRate, inflationExpectations }` — from inflation tick
- `fiscalPublicGoodsQuality: { infrastructureQuality, educationQuality, welfareQuality, defenseQuality }` — from fiscal tick (lifted to outer scope per Phase 3 convention)
- `resolution.lifecycleEvents` — death events for peer-death signal
</interfaces>

<strip_table>
<!-- Actions to strip (from 11-RESEARCH.md §1). Reference when editing physicsEngine.ts. -->

Cortisol relief to zero (D-01):
| Action                | Current cor | New cor |
|-----------------------|-------------|---------|
| WORK                  | −3          | 0       |
| WORK_AT_ENTERPRISE    | −3          | 0       |
| REST                  | −5          | 0       |
| PRODUCE_AND_SELL      | −2          | 0       |
| POST_BUY_ORDER        | −1          | 0       |
| POST_SELL_ORDER       | −1          | 0       |
| HELP                  | −5          | 0       |
| DEPOSIT               | −2          | 0       |
| REPAY_LOAN            | −3          | 0       |
| BUY_BOND              | −1          | 0       |
| ISSUE_GOV_BOND        | −2          | 0       |

Happiness reward to zero (D-06):
| Action                | Current hap | New hap |
|-----------------------|-------------|---------|
| REST                  | +2          | 0       |
| POST_BUY_ORDER        | +1          | 0       |
| POST_SELL_ORDER       | +1          | 0       |
| PRODUCE_AND_SELL      | +1          | 0       |
| DEPOSIT               | +1          | 0       |
| REPAY_LOAN            | +3          | 0       |
| BUY_SHARES            | +1          | 0       |
| BUY_BOND              | +1          | 0       |

KEEP (outcome-driven, unchanged):
- STEAL (cor +10, hap −3), STRIKE (+5/+5), SABOTAGE (+18/+5), EMBEZZLE (+20/+2)
- QUIT_JOB (cor +4, hap −1) — asserted in edgeCases.test.ts
- INVEST (cor +3), FOUND_ENTERPRISE (cor +5, hap +3), HIRE/FIRE (cor +2, hap +2)
- HELP hap +5 (altruistic outcome retained even though cor is stripped)
- NONE/idle (cor +2, hap −1) — purposelessness outcome
- TAKE_LOAN (cor +5), SELL_SHARES (cor +1, hap −1)
- ISSUE_LOAN, SET_INTEREST_RATE, SET_RESERVE_RATIO, SET_BASE_RATE (bank/central only)
- ADJUST_TAX, SUPPRESS, APPLY_FOR_JOB, WITHDRAW
</strip_table>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip per-action cortisol/happiness rewards in physicsEngine.ts and update trace strings</name>
  <read_first>
    - server/src/mechanics/physicsEngine.ts (lines 161-498 hold action switch; read entire file for context)
    - server/src/mechanics/__tests__/edgeCases.test.ts (line 39-57 asserts QUIT_JOB deltas — must NOT regress)
    - server/src/mechanics/__tests__/physicsEngine.cortisolStrip.test.ts (Wave 0 scaffold; flip `it.todo` → real assertions)
    - server/src/mechanics/__tests__/physicsEngine.happinessStrip.test.ts (Wave 0 scaffold)
    - .planning/phases/11-simulation-realism-organic-stress-pressure-fiscal-balance-and-sfc-leak-closure/11-RESEARCH.md §1 (full action strip table) and §Pitfall 1 (keep trace lines, rewrite strings only)
  </read_first>
  <behavior>
    - Test: `applyPhysics(agent, { type: 'WORK', ... })` returns `{ cortisolDelta: 0, happinessDelta: -1, healthDelta: -2, wealthDelta: roleIncome }` (cor stripped, h/hap unchanged, w unchanged)
    - Test: `applyPhysics(agent, { type: 'REST', ... })` returns `{ cortisolDelta: 0, happinessDelta: 0, healthDelta: +5 }`
    - Test: `applyPhysics(agent, { type: 'HELP', ... })` returns `{ cortisolDelta: 0, happinessDelta: +5, healthDelta: 0, wealthDelta: -5 }` (cor stripped, altruistic hap retained)
    - Test: `applyPhysics(agent, { type: 'STEAL', ... })` unchanged: `cortisolDelta: 10, happinessDelta: -3`
    - Test: `applyPhysics(agent, { type: 'QUIT_JOB', ... })` unchanged: `cortisolDelta: 4, happinessDelta: -1` (per edgeCases.test.ts)
    - Test: Trace log for WORK contains a reworded string (not the old "productive decompression" type language); e.g., "Δcortisol: 0 (per-action relief removed; structural pressures apply at tick end)"
  </behavior>
  <action>
    Edit `server/src/mechanics/physicsEngine.ts` action switch (lines 161-498):

    1. For each action in the "Cortisol relief to zero" table (WORK, WORK_AT_ENTERPRISE, REST, PRODUCE_AND_SELL, POST_BUY_ORDER, POST_SELL_ORDER, HELP, DEPOSIT, REPAY_LOAN, BUY_BOND, ISSUE_GOV_BOND):
       - Change `cortisolDelta: -N` → `cortisolDelta: 0` in the return object
       - Keep the `trace.push(...)` line but REWRITE the string to describe the new model:
         - Before: `trace.push('[PHYSICS] WORK: productive decompression, Δcortisol: -3')`
         - After: `trace.push('[PHYSICS] WORK: Δcortisol: 0 (per-action relief removed; structural pressures apply at tick end)')`
       - Keep health/wealth/happiness deltas unchanged (unless listed in the happiness strip table).
       - DO NOT delete the trace.push line (Pitfall 1 — PhysicsLaboratory UI consumes it).

    2. For each action in the "Happiness reward to zero" table (REST, POST_BUY_ORDER, POST_SELL_ORDER, PRODUCE_AND_SELL, DEPOSIT, REPAY_LOAN, BUY_SHARES, BUY_BOND):
       - Change `happinessDelta: +N` → `happinessDelta: 0`
       - Rewrite trace string similarly.

    3. For HELP specifically: cor stripped to 0; hap stays at +5 (altruistic outcome); w stays at -5; h stays at 0.

    4. DO NOT touch: STEAL, STRIKE, SABOTAGE, EMBEZZLE, INVEST, QUIT_JOB, FOUND_ENTERPRISE, HIRE_EMPLOYEE, FIRE_EMPLOYEE, POST_JOB_OFFER, APPLY_FOR_JOB, TAKE_LOAN, WITHDRAW, SELL_SHARES, NONE, ISSUE_LOAN, SET_INTEREST_RATE, SET_RESERVE_RATIO, SET_BASE_RATE, ADJUST_TAX, SUPPRESS. These are outcome-driven per 11-RESEARCH.md §1.

    5. After editing, convert Wave 0 scaffold tests from `it.todo` to real `it` assertions:
       - `server/src/mechanics/__tests__/physicsEngine.cortisolStrip.test.ts`: for each of the 11 stripped cortisol actions, write `expect(delta.cortisolDelta).toBe(0)` and for retained actions (STEAL, QUIT_JOB) write `expect(delta.cortisolDelta).toBe(10)` / `toBe(4)` as regression guards.
       - `server/src/mechanics/__tests__/physicsEngine.happinessStrip.test.ts`: same pattern for 8 stripped happiness actions + HELP retention (`expect(delta.happinessDelta).toBe(5)`).

    Use the agent fixture factory from `server/src/mechanics/physics_sandbox.ts` for test setup (mentioned in 11-RESEARCH.md §Wave 0).
  </action>
  <verify>
    <automated>npx vitest run server/src/mechanics/__tests__/physicsEngine.cortisolStrip.test.ts server/src/mechanics/__tests__/physicsEngine.happinessStrip.test.ts server/src/mechanics/__tests__/edgeCases.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cortisolDelta: -3" server/src/mechanics/physicsEngine.ts` returns 0 (all -3 relief gone)
    - `grep -c "cortisolDelta: -5" server/src/mechanics/physicsEngine.ts` returns 0 (REST/HELP -5 gone)
    - `grep -c "cortisolDelta: -2" server/src/mechanics/physicsEngine.ts` returns 0 (PRODUCE_AND_SELL/DEPOSIT/ISSUE_GOV_BOND gone)
    - `grep -c "cortisolDelta: -1" server/src/mechanics/physicsEngine.ts` returns 0 (POST_BUY/POST_SELL/BUY_BOND gone)
    - `grep -c "cortisolDelta: 10" server/src/mechanics/physicsEngine.ts` returns &gt;= 1 (STEAL retained)
    - `grep -c "cortisolDelta: 4" server/src/mechanics/physicsEngine.ts` returns &gt;= 1 (QUIT_JOB retained)
    - `grep -cE "happinessDelta:\s*\+?[123]" server/src/mechanics/physicsEngine.ts` shows retained values for HELP (+5), STRIKE (+5), FOUND_ENTERPRISE (+3), HIRE (+2), POST_JOB_OFFER (+2), APPLY_FOR_JOB (+1)
    - `grep -c "per-action relief removed" server/src/mechanics/physicsEngine.ts` returns &gt;= 11 (one per stripped action trace line)
    - `npx vitest run server/src/mechanics/__tests__/physicsEngine.cortisolStrip.test.ts -x` exits 0 with real (not todo) assertions passing
    - `npx vitest run server/src/mechanics/__tests__/physicsEngine.happinessStrip.test.ts -x` exits 0
    - `npx vitest run server/src/mechanics/__tests__/edgeCases.test.ts -x` exits 0 (QUIT_JOB deltas still 4/-1)
  </acceptance_criteria>
  <done>11 cortisol strips and 8 happiness strips landed; trace log strings reworded without deletion; Wave 0 strip-test scaffolds converted to passing real assertions; edgeCases regression guard still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add structural pressure injection loop in simulationRunner.ts</name>
  <read_first>
    - server/src/orchestration/simulationRunner.ts (focus on lines 2375-2475 inflation tick and 2476-2566 post-inflation region; also lines 1812-1900 final stat commit)
    - server/src/mechanics/__tests__/structuralPressures.test.ts (Wave 0 scaffold)
    - server/src/orchestration/__tests__/statClamping.test.ts (Wave 0 scaffold)
    - .planning/phases/11-simulation-realism-organic-stress-pressure-fiscal-balance-and-sfc-leak-closure/11-RESEARCH.md §2 and §Example 1 (copy the structural pressure loop verbatim)
    - .planning/phases/11-simulation-realism-organic-stress-pressure-fiscal-balance-and-sfc-leak-closure/11-RESEARCH.md §Pitfall 2 (bank/central exclusion) and §Pitfall 5 (governanceEnabled !== false pattern)
  </read_first>
  <behavior>
    - Test: After one iteration with high inflation surprise (5%) and high Gini (0.5), bottom-quintile citizens have cortisolDelta increased by expected amount (k_inflation_cor × 5 + k_gini_cor × max(0, 0.5-0.35)); top-quintile citizens get only the inflation + pg + unemp contributions (no Gini)
    - Test: Bank agent (type='bank') receives ZERO structural pressure contribution
    - Test: Central bank agent (role='central_bank') receives ZERO structural pressure contribution
    - Test: When `deathsThisTick = 2`, every alive citizen's happinessDelta decreases by `2 × k_peer_death_hap = 4`
    - Test: Final agent.currentStats.cortisol is clamped to [3, 95]; happiness clamped to [5, 95]
    - Test: If `physicsConfig.k_inflation_cor = 0`, no inflation-surprise cortisol pressure is applied (config hot-swap works)
  </behavior>
  <action>
    Edit `server/src/orchestration/simulationRunner.ts`:

    1. Locate the block AFTER the inflation tick (around line 2475-2500, after `inflationOutput` is assigned and BEFORE the telemetry construction at :2592). This is the injection point per 11-RESEARCH.md §2 recommendation.

    2. Insert the structural pressure loop EXACTLY as specified in 11-RESEARCH.md §Example 1:
    ```typescript
    // ── Phase 11 D-02 / D-07: Structural pressure injection ──
    if (inflationOutput) {
      const surprise = Math.max(0, inflationOutput.inflationRate - inflationOutput.inflationExpectations);
      const giniExcess = Math.max(0, giniCoefficient - physicsConfig.giniStressThreshold);
      const deathsThisTick = resolution.lifecycleEvents?.filter(e =&gt; e.type === 'death').length ?? 0;

      const sortedWealth = [...statUpdates].sort((a, b) =&gt; a.wealth - b.wealth);
      const bottomQuintileCutoff = sortedWealth[Math.floor(sortedWealth.length * 0.2)]?.wealth ?? 0;

      const pg = fiscalPublicGoodsQuality;
      const pgPressure = pg
        ? (1 - Math.min(pg.infrastructureQuality, pg.educationQuality, pg.welfareQuality) / 50)
        : 1;

      for (const agent of aliveAgents) {
        // D-02/D-07 exclude bank and central_bank agents (Pitfall 2)
        if (agent.type === 'bank' || agent.role?.toLowerCase() === 'central_bank') continue;
        const weekState = weekStateMap.get(agent.id);
        if (!weekState) continue;

        const isBottomQuintile = agent.currentStats.wealth &lt;= bottomQuintileCutoff;
        const isUnemployed = !employmentRegistry.has(agent.id);
        const lowWealth = agent.currentStats.wealth &lt; physicsConfig.lowWealthThreshold;

        // Cortisol pressures (D-02)
        weekState.cortisolDelta += physicsConfig.k_inflation_cor * surprise;
        if (isBottomQuintile) weekState.cortisolDelta += physicsConfig.k_gini_cor * giniExcess;
        if (isUnemployed &amp;&amp; lowWealth) weekState.cortisolDelta += physicsConfig.k_unemp_cor;
        weekState.cortisolDelta += physicsConfig.k_pg_cor * pgPressure;

        // Happiness pressures (D-07)
        weekState.happinessDelta -= physicsConfig.k_peer_death_hap * deathsThisTick;
        if (isBottomQuintile) weekState.happinessDelta -= physicsConfig.k_gini_hap * giniExcess;
        if (isUnemployed &amp;&amp; lowWealth) weekState.happinessDelta -= physicsConfig.k_unemp_hap;
        if (pg) weekState.happinessDelta -= physicsConfig.k_welfare_hap * (1 - pg.welfareQuality / 50);
        weekState.happinessDelta -= physicsConfig.k_inflation_hap * surprise;
      }
    }
    ```

    3. Verify `fiscalPublicGoodsQuality` is in scope at this line. Per STATE.md Phase 03: "fiscalPublicGoodsQuality lifted to outer scope so iterTelemetry can read quality values". If not visible, check if it needs to be declared earlier or fetched from `fiscalDelta.updatedPublicGoods`. Hoist if needed.

    4. Update clamp bounds at final stat commit sites (around line 1812+). Cortisol: `Math.max(physicsConfig.cortisolFloor, Math.min(physicsConfig.cortisolCeiling, newCortisol))` → [3, 95]. Happiness: `Math.max(physicsConfig.happinessFloor, Math.min(physicsConfig.happinessCeiling, newHappiness))` → [5, 95]. Per STATE.md Phase 10-GC3 convention: apply the floor only at final commit sites, not on the `runningCortisol` accumulator (to match existing CORTISOL_FLOOR=3 convention). CONTEXT.md `Claude's Discretion` bullet permits this choice; stay consistent.

       Grep for existing clamp sites: `grep -n "CORTISOL_FLOOR\|clampStat\|Math.max.*cortisol\|Math.min.*cortisol" server/src/orchestration/simulationRunner.ts`. Update each to use `physicsConfig.cortisolCeiling` / `cortisolFloor` / `happinessCeiling` / `happinessFloor` rather than literals.

    5. Convert `server/src/mechanics/__tests__/structuralPressures.test.ts` scaffold to real assertions:
       - Test each of the 4 cortisol pressures independently (set k_gini_cor=0, k_unemp_cor=0, etc. via `updatePhysicsConfig` to isolate one signal at a time)
       - Test bank-agent exclusion: create a test agent with `type: 'bank'`, call the runner's structural pressure logic (extract helper if needed), assert its cortisolDelta is 0
       - Test bottom-quintile gating: 10 agents with wealths [10..100], assert only the 2 lowest get Gini pressure
       - Test peer-death: 2 deaths → happinessDelta decreased by `2 × k_peer_death_hap` for all alive citizens

       If the pressure loop is hard to test in isolation (inlined in runner), extract it to a helper function `applyStructuralPressures(context)` in `server/src/orchestration/helpers/structuralPressures.ts` that takes a context object and mutates `weekStateMap`. Import and call from runner. Helper is easier to unit test.

    6. Convert `server/src/orchestration/__tests__/statClamping.test.ts` scaffold to real assertions:
       - Agent with runningCortisol=200 → final stat clamped to 95
       - Agent with runningCortisol=-10 → final stat clamped to 3
       - Agent with runningHappiness=200 → final stat clamped to 95
       - Agent with runningHappiness=-10 → final stat clamped to 5
  </action>
  <verify>
    <automated>npx vitest run server/src/mechanics/__tests__/structuralPressures.test.ts server/src/orchestration/__tests__/statClamping.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "physicsConfig.k_inflation_cor" server/src/orchestration/simulationRunner.ts` returns &gt;= 1 (OR count &gt;= 1 in `server/src/orchestration/helpers/structuralPressures.ts` if extracted)
    - `grep -c "physicsConfig.k_peer_death_hap" server/src/orchestration/simulationRunner.ts` (or helper) returns &gt;= 1
    - `grep -E "type\s*===\s*'bank'" server/src/orchestration/simulationRunner.ts` (or helper) matches (bank exclusion present)
    - `grep -E "role\?\.toLowerCase\(\)\s*===\s*'central_bank'" server/src/orchestration/simulationRunner.ts` (or helper) matches
    - `grep -c "physicsConfig.cortisolCeiling" server/src/orchestration/simulationRunner.ts` returns &gt;= 1
    - `grep -c "physicsConfig.happinessFloor" server/src/orchestration/simulationRunner.ts` returns &gt;= 1
    - `npx vitest run server/src/mechanics/__tests__/structuralPressures.test.ts -x` exits 0 with real assertions (no `it.todo` remaining)
    - `npx vitest run server/src/orchestration/__tests__/statClamping.test.ts -x` exits 0
    - `npm run test -w server` exits 0 (no existing test broken by clamp change or new pressures)
    - `npx vitest run server/src/__tests__/sfcBanking.test.ts server/src/__tests__/sfcFiscal.test.ts server/src/__tests__/sfcInflation.test.ts -x` all exit 0 (SFC unchanged)
  </acceptance_criteria>
  <done>Structural pressure loop applies 4 cortisol and 5 happiness signals to citizens per tick; bank/central excluded; clamps use physicsConfig bounds [3,95] and [5,95]; full test suite green; SFC unaffected.</done>
</task>

</tasks>

<verification>
- Both tasks committed sequentially.
- Full suite: `npm run test -w server` exits 0.
- Grep: cortisol/happiness strip completeness: `grep -c "cortisolDelta: -" server/src/mechanics/physicsEngine.ts` returns only the count of retained negative values (STEAL=0 since it's +10, HELP w=-5, etc.). Expected: 0 for `cortisolDelta: -3` and `-2` and `-5` (stripped), remaining count matches STEAL happiness (-3 / -5 on wealth/health).
- SFC tests unchanged: `npx vitest run server/src/__tests__/sfcBanking.test.ts server/src/__tests__/sfcFiscal.test.ts server/src/__tests__/sfcInflation.test.ts server/src/__tests__/sfcCapitalMarkets.test.ts server/src/__tests__/sfcInvariant.test.ts -x` exits 0.
- Smoke manual run: bootstrap a session, run 3 iterations, observe cortisol does not immediately decay to floor (per CONTEXT.md §Specifics "utopia-baseline evidence").
</verification>

<success_criteria>
- Running the existing United States bootstrap scenario for 5 iterations: cortisol trajectory is non-monotonic (no longer decays to floor as in `Results/session-united-states.json`).
- SFC invariant preserved: all SFC tests green.
- `edgeCases.test.ts` regression guard (QUIT_JOB cor=4, hap=-1) still green — no outcome-driven delta was accidentally stripped.
- Structural pressures respond to hot-swapped coefficients at runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/11-simulation-realism-organic-stress-pressure-fiscal-balance-and-sfc-leak-closure/11-02-SUMMARY.md`.
</output>
