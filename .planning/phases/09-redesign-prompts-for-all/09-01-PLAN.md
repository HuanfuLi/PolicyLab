---
phase: 09-redesign-prompts-for-all
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/llm/prompts.ts
  - server/src/llm/prompts/index.ts
  - server/src/llm/prompts/agent-intent.ts
  - server/src/llm/prompts/central-agent.ts
  - server/src/llm/prompts/reflection.ts
  - server/src/llm/prompts/governance.ts
  - server/src/llm/prompts/comparison.ts
  - server/src/llm/prompts/location.ts
  - server/src/llm/prompts/shared.ts
  - server/src/llm/centralAgent.ts
  - server/src/orchestration/simulationRunner.ts
  - server/src/orchestration/reflectionRunner.ts
  - server/src/orchestration/governanceManager.ts
  - server/src/routes/bootstrap.ts
  - server/src/routes/compare.ts
  - server/src/routes/review.ts
  - server/src/__tests__/sfcInflation.test.ts
autonomous: true
requirements: [D-19, D-20]

must_haves:
  truths:
    - "All 20+ exported functions and types from prompts.ts are accessible via prompts/index.ts barrel"
    - "All 9 consumer files compile successfully with updated import paths"
    - "No function signatures or content changed during the split"
    - "TypeScript build succeeds with zero errors"
    - "All 195+ existing tests pass unchanged"
  artifacts:
    - path: "server/src/llm/prompts/index.ts"
      provides: "Barrel re-export of all prompt modules"
      exports: ["buildNaturalIntentPrompt", "buildBrainstormMessages", "buildResolutionPrompt", "buildAgentRosterMessages", "ACTION_SCHEMAS", "buildActionDictionary"]
    - path: "server/src/llm/prompts/agent-intent.ts"
      provides: "Citizen decision prompts"
      exports: ["buildNaturalIntentPrompt", "buildIntentPrompt"]
    - path: "server/src/llm/prompts/central-agent.ts"
      provides: "Central Agent prompts"
      exports: ["buildBrainstormMessages", "buildOverviewMessages", "buildLawMessages", "buildAgentRosterMessages", "buildResolutionPrompt", "buildFinalReportPrompt", "buildGroupResolutionMessages", "buildMergeResolutionMessages", "buildRefineMessages"]
    - path: "server/src/llm/prompts/reflection.ts"
      provides: "Reflection and review prompts"
      exports: ["buildAgentReflectionPrompt", "buildAgentReflection2Prompt", "buildEvaluationPrompt", "buildReviewChatPrompt", "buildPostMortemPrompt"]
    - path: "server/src/llm/prompts/governance.ts"
      provides: "Governance prompts"
      exports: ["buildProposalPrompt", "buildBallotPrompt", "buildVotePrompt", "buildFranchiseSizePrompt", "buildLegalityCheckPrompt"]
    - path: "server/src/llm/prompts/comparison.ts"
      provides: "Comparison prompts"
      exports: ["buildComparisonMessages", "buildComparisonChatMessages"]
    - path: "server/src/llm/prompts/location.ts"
      provides: "Location-based prompts"
      exports: ["buildLocationAgentRosterMessages", "buildLocationLawMessages", "buildScenarioInterpretationMessages"]
    - path: "server/src/llm/prompts/shared.ts"
      provides: "Shared types, interfaces, action schemas, helper builders"
      exports: ["ACTION_SCHEMAS", "buildActionDictionary", "buildMarketBoardSection", "buildEmploymentBoardSection", "buildPersonalStatusSection", "QueuedActionInstruction", "CitizenDecisionOutput", "MarketBoardEntry", "EmploymentBoardEntry", "PersonalStatusBoard", "CitizenBankingContext", "BankOperationsContext", "CitizenCapitalMarketContext", "CitizenFiscalContext", "CentralBankContext", "AgentIntent", "PostMortemInput", "GovernancePolicyProposal", "GovernanceBallotItem"]
  key_links:
    - from: "server/src/llm/prompts/index.ts"
      to: "all 7 sub-modules"
      via: "export * from './module.js'"
      pattern: "export \\* from"
    - from: "server/src/llm/centralAgent.ts"
      to: "server/src/llm/prompts/index.ts"
      via: "import path update"
      pattern: "from './prompts/index.js'"
---

<objective>
Split the monolithic prompts.ts (2025 lines) into 7 domain-specific modules under a `prompts/` directory with a barrel re-export index.ts.

Purpose: Enable parallel work on citizen intent prompts vs. central agent prompts vs. other prompt domains in Plans 02 and 03. The split is pure mechanical refactoring with zero content changes.

Output: 7 module files + barrel index.ts, all consumer imports updated, original prompts.ts deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-redesign-prompts-for-all/09-CONTEXT.md
@.planning/phases/09-redesign-prompts-for-all/09-RESEARCH.md
@server/src/llm/prompts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 prompt modules + barrel index</name>
  <files>
    server/src/llm/prompts/shared.ts,
    server/src/llm/prompts/agent-intent.ts,
    server/src/llm/prompts/central-agent.ts,
    server/src/llm/prompts/reflection.ts,
    server/src/llm/prompts/governance.ts,
    server/src/llm/prompts/comparison.ts,
    server/src/llm/prompts/location.ts,
    server/src/llm/prompts/index.ts
  </files>
  <read_first>
    server/src/llm/prompts.ts
  </read_first>
  <action>
    Create directory `server/src/llm/prompts/`.

    Per D-19, split prompts.ts functions into 7 files. Per D-20, template strings stay inline in builder functions (no external template files).

    **shared.ts** — Move these items (lines ~338-707 of prompts.ts):
    - `ActionSchema` interface (private, not exported)
    - `ACTION_SCHEMAS` constant (export)
    - `buildActionDictionary` function (export)
    - All exported interfaces: `QueuedActionInstruction`, `CitizenDecisionOutput`, `MarketBoardEntry`, `EmploymentBoardEntry`, `PersonalStatusBoard`, `CitizenBankingContext`, `BankOperationsContext`, `CitizenCapitalMarketContext`, `CitizenFiscalContext`, `CentralBankContext`, `AgentIntent`, `PostMortemInput`, `GovernancePolicyProposal`, `GovernanceBallotItem`
    - All helper builders: `buildMarketBoardSection`, `buildEmploymentBoardSection`, `buildPersonalStatusSection`, `buildCitizenBankingSection`, `buildBankOperationsSection`, `buildCitizenCapitalMarketSection`, `buildCitizenFiscalSection`, `buildInflationContextSection`, `buildCentralBankSection`
    - Import `ActionCode` from `../../mechanics/actionCodes.js`
    - Import types from `@policylab/shared` as needed

    **agent-intent.ts** — Move these items (lines ~209-1000 of prompts.ts):
    - `buildIntentPrompt` function
    - `buildNaturalIntentPrompt` function
    - Import shared types/helpers from `./shared.js`
    - Import `ActionCode` from `../../mechanics/actionCodes.js`
    - Import `Agent`, `Session`, `ContentBlock`, `LLMMessage` from `@policylab/shared`

    **central-agent.ts** — Move these items (lines ~15-207, ~1002-1650 of prompts.ts):
    - `buildBrainstormMessages`, `buildOverviewMessages`, `buildLawMessages`, `buildAgentRosterMessages`
    - `buildResolutionPrompt`, `buildFinalReportPrompt`
    - `buildGroupResolutionMessages`, `buildMergeResolutionMessages`, `buildRefineMessages`
    - Import `LLMMessage`, `Agent`, `Session`, `ContentBlock` from `@policylab/shared`
    - Import `AgentIntent` from `./shared.js`

    **reflection.ts** — Move these items (lines ~1126-1352 of prompts.ts):
    - `buildAgentReflectionPrompt`, `buildAgentReflection2Prompt`
    - `buildEvaluationPrompt`, `buildReviewChatPrompt`
    - `buildPostMortemPrompt` (uses `PostMortemInput` from shared)
    - Import `PostMortemInput` from `./shared.js`
    - Import `LLMMessage`, `Agent`, `Session` from `@policylab/shared`

    **governance.ts** — Move these items (lines ~1652-1893 of prompts.ts):
    - `buildProposalPrompt`, `buildBallotPrompt`, `buildVotePrompt`
    - `buildFranchiseSizePrompt`, `buildLegalityCheckPrompt`
    - Import `GovernancePolicyProposal`, `GovernanceBallotItem` from `./shared.js`
    - Import `LLMMessage` from `@policylab/shared`

    **comparison.ts** — Move these items (lines ~1354-1449 of prompts.ts):
    - `buildComparisonMessages`, `buildComparisonChatMessages`
    - Import `LLMMessage` from `@policylab/shared`

    **location.ts** — Move these items (lines ~1895-2025 of prompts.ts):
    - `buildLocationAgentRosterMessages`, `buildLocationLawMessages`, `buildScenarioInterpretationMessages`
    - Import `LLMMessage` from `@policylab/shared`

    **index.ts** — Barrel re-export:
    ```typescript
    export * from './shared.js';
    export * from './agent-intent.js';
    export * from './central-agent.js';
    export * from './reflection.js';
    export * from './governance.js';
    export * from './comparison.js';
    export * from './location.js';
    ```

    CRITICAL: No function content changes. Copy-paste exact code. Only change import paths within the new files. Every export that existed in prompts.ts must be re-exported through index.ts.
  </action>
  <verify>
    <automated>cd C:/Users/16079/Code/PolicyLab && npx tsc --noEmit -p server/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - server/src/llm/prompts/index.ts exists and contains `export * from './shared.js'`, `export * from './agent-intent.js'`, `export * from './central-agent.js'`, `export * from './reflection.js'`, `export * from './governance.js'`, `export * from './comparison.js'`, `export * from './location.js'`
    - server/src/llm/prompts/shared.ts contains `export const ACTION_SCHEMAS`
    - server/src/llm/prompts/agent-intent.ts contains `export function buildNaturalIntentPrompt`
    - server/src/llm/prompts/central-agent.ts contains `export function buildBrainstormMessages`
    - server/src/llm/prompts/reflection.ts contains `export function buildAgentReflectionPrompt`
    - server/src/llm/prompts/governance.ts contains `export function buildProposalPrompt`
    - server/src/llm/prompts/comparison.ts contains `export function buildComparisonMessages`
    - server/src/llm/prompts/location.ts contains `export function buildLocationAgentRosterMessages`
    - server/src/llm/prompts.ts is deleted (file no longer exists)
  </acceptance_criteria>
  <done>All 7 module files + barrel index created. Original prompts.ts deleted. TypeScript compiles with zero errors from the new module structure.</done>
</task>

<task type="auto">
  <name>Task 2: Update all consumer import paths</name>
  <files>
    server/src/llm/centralAgent.ts,
    server/src/orchestration/simulationRunner.ts,
    server/src/orchestration/reflectionRunner.ts,
    server/src/orchestration/governanceManager.ts,
    server/src/routes/bootstrap.ts,
    server/src/routes/compare.ts,
    server/src/routes/review.ts,
    server/src/__tests__/sfcInflation.test.ts
  </files>
  <read_first>
    server/src/llm/centralAgent.ts,
    server/src/orchestration/simulationRunner.ts,
    server/src/orchestration/reflectionRunner.ts,
    server/src/orchestration/governanceManager.ts,
    server/src/routes/bootstrap.ts,
    server/src/routes/compare.ts,
    server/src/routes/review.ts,
    server/src/__tests__/sfcInflation.test.ts
  </read_first>
  <action>
    Update import paths in all 8 consumer files. Each file's import from `'./prompts.js'` or `'../llm/prompts.js'` must change to `'./prompts/index.js'` or `'../llm/prompts/index.js'` respectively.

    Specific changes:

    1. **centralAgent.ts** line 27: `from './prompts.js'` -> `from './prompts/index.js'`
    2. **simulationRunner.ts** line 45: `from '../llm/prompts.js'` -> `from '../llm/prompts/index.js'`
    3. **reflectionRunner.ts** line 23: `from '../llm/prompts.js'` -> `from '../llm/prompts/index.js'`
    4. **governanceManager.ts** lines 21-22: both `from '../llm/prompts.js'` -> `from '../llm/prompts/index.js'`
    5. **bootstrap.ts** line 29: `from '../llm/prompts.js'` -> `from '../llm/prompts/index.js'`
    6. **compare.ts** line 16: `from '../llm/prompts.js'` -> `from '../llm/prompts/index.js'`
    7. **review.ts** line 16: `from '../llm/prompts.js'` -> `from '../llm/prompts/index.js'`
    8. **sfcInflation.test.ts** line 4: `from '../llm/prompts.js'` -> `from '../llm/prompts/index.js'`

    Do NOT change any imported names -- only the path string. All `import { X } from` and `import type { Y } from` statements keep their named imports identical.
  </action>
  <verify>
    <automated>cd C:/Users/16079/Code/PolicyLab && npm run build 2>&1 | tail -5 && npm run test -w server -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - centralAgent.ts contains `from './prompts/index.js'`
    - simulationRunner.ts contains `from '../llm/prompts/index.js'`
    - reflectionRunner.ts contains `from '../llm/prompts/index.js'`
    - governanceManager.ts contains `from '../llm/prompts/index.js'`
    - bootstrap.ts contains `from '../llm/prompts/index.js'`
    - compare.ts contains `from '../llm/prompts/index.js'`
    - review.ts contains `from '../llm/prompts/index.js'`
    - sfcInflation.test.ts contains `from '../llm/prompts/index.js'`
    - No file contains `from './prompts.js'` or `from '../llm/prompts.js'` (old paths eliminated)
    - `npm run build` exits 0
    - `npm run test -w server -- --run` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 8 consumer files import from the new barrel path. TypeScript build succeeds. All existing tests pass. The file split is complete with zero behavioral changes.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 — TypeScript compilation succeeds across all workspaces
2. `npm run test -w server -- --run` — all 195+ tests pass
3. `ls server/src/llm/prompts/` shows 8 files (7 modules + index.ts)
4. `ls server/src/llm/prompts.ts` returns "no such file" (original deleted)
5. No consumer file references the old `prompts.js` path
</verification>

<success_criteria>
- The monolithic 2025-line prompts.ts is split into 7 domain modules per D-19
- Template strings remain inline per D-20
- All consumers compile and tests pass — zero behavioral change
- Plans 02 and 03 can now work on separate module files in parallel
</success_criteria>

<output>
After completion, create `.planning/phases/09-redesign-prompts-for-all/09-01-SUMMARY.md`
</output>
