---
phase: 09-redesign-prompts-for-all
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - server/src/llm/prompts/central-agent.ts
  - server/src/llm/prompts/reflection.ts
  - server/src/llm/prompts/location.ts
  - server/src/llm/__tests__/promptContent.test.ts
autonomous: true
requirements: [D-11, D-12, D-13, D-14]

must_haves:
  truths:
    - "buildAgentRosterMessages requests 5-8 sentence life stories with childhood, formative events, economic philosophy"
    - "Central Agent resolution prompt retains mechanical framing ([PHYSICS LOG], [OBJECTIVE SYSTEM METRICS])"
    - "NARRATIVE DIRECTIVE friction-first bias preserved unchanged"
    - "Citizen-facing reflection prompts use immersive first-person framing"
    - "Central Agent prompts do NOT get immersion rewrites"
    - "buildLocationAgentRosterMessages also requests 5-8 sentence life stories"
  artifacts:
    - path: "server/src/llm/prompts/central-agent.ts"
      provides: "Updated roster prompt (D-11/D-12) + preserved resolution prompts (D-13/D-14)"
      exports: ["buildAgentRosterMessages", "buildResolutionPrompt", "buildGroupResolutionMessages"]
    - path: "server/src/llm/prompts/reflection.ts"
      provides: "Immersive reflection prompts for citizen agents"
      exports: ["buildAgentReflectionPrompt", "buildAgentReflection2Prompt", "buildReviewChatPrompt", "buildPostMortemPrompt"]
    - path: "server/src/llm/prompts/location.ts"
      provides: "Location roster prompt with enriched 5-8 sentence life story format"
      exports: ["buildLocationAgentRosterMessages", "buildLocationLawMessages", "buildScenarioInterpretationMessages"]
  key_links:
    - from: "buildAgentRosterMessages"
      to: "agent.background field"
      via: "LLM-generated roster with rich life stories"
      pattern: "5-8 sentence"
    - from: "buildResolutionPrompt"
      to: "mechanical framing"
      via: "preserved [PHYSICS LOG] and system metrics"
      pattern: "PHYSICS"
    - from: "buildLocationAgentRosterMessages"
      to: "agent.background field"
      via: "LLM-generated roster with rich life stories (location mode)"
      pattern: "5-8 sentence"
---

<objective>
Update agent roster generation to produce rich 5-8 sentence life stories, and align reflection prompts with the immersion principles. Central Agent prompts are explicitly preserved per D-13.

Purpose: Richer agent backgrounds drive differentiated emergent behavior -- a farmer who hoards food due to past trauma behaves differently from one who shares. Reflection prompts must match the immersive tone of the rewritten intent prompts.

Output: Updated central-agent.ts (roster prompt), location.ts (location roster prompt), and reflection.ts (citizen-facing reflection prompts).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-redesign-prompts-for-all/09-CONTEXT.md
@.planning/phases/09-redesign-prompts-for-all/09-RESEARCH.md
@.planning/phases/09-redesign-prompts-for-all/09-01-SUMMARY.md
@server/src/llm/prompts/central-agent.ts
@server/src/llm/prompts/reflection.ts
@server/src/llm/prompts/location.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich agent roster background prompt in central-agent.ts and location.ts</name>
  <files>server/src/llm/prompts/central-agent.ts, server/src/llm/prompts/location.ts</files>
  <read_first>
    server/src/llm/prompts/central-agent.ts,
    server/src/llm/prompts/location.ts
  </read_first>
  <action>
    Per D-11 and D-12, update `buildAgentRosterMessages` in central-agent.ts to request rich life stories instead of "1-2 sentence background".

    In the system prompt's JSON schema example, change:
    ```
    "background": "string - 1-2 sentence background"
    ```
    to:
    ```
    "background": "string - 5-8 sentence life story including: where they grew up, a formative event that shaped their worldview, their economic philosophy (do they hoard, share, trade aggressively, save cautiously?), a personal fear or ambition that drives them, and specific practical knowledge tied to their role (a farmer knows that producing 20 units of food costs a week of labor; a merchant knows prices rise when supply is scarce)"
    ```

    Also update the Rules section. Change:
    ```
    - Stats should vary realistically based on role and background
    ```
    to:
    ```
    - Stats should vary realistically based on role and background
    - Each background MUST include an economic instinct: how this person relates to money, food, and risk. Examples: 'He hoards food instinctively after a childhood famine', 'She trusts no bank after her father lost his savings', 'He invests aggressively, chasing every opportunity'
    - Backgrounds should create natural economic diversity: some agents produce, some trade, some save, some spend freely
    ```

    **Also update `buildLocationAgentRosterMessages` in location.ts** with the same background schema change (5-8 sentence life story format). Read location.ts to find the equivalent background field and apply the identical changes:
    - Change background field description to "5-8 sentence life story" matching the format above
    - Add the economic instinct requirement to the Rules section

    Do NOT modify `buildLocationLawMessages` or `buildScenarioInterpretationMessages` in location.ts -- these are system/analytical prompts, not citizen-facing.

    Per D-13: Do NOT modify `buildResolutionPrompt`, `buildGroupResolutionMessages`, `buildMergeResolutionMessages`, or `buildRefineMessages` in central-agent.ts. These are Central Agent prompts that KEEP mechanical framing.

    Per D-14: Do NOT modify any NARRATIVE DIRECTIVE text that contains "FRICTION" or "INEQUALITY" or "DISRUPTION".
  </action>
  <verify>
    <automated>cd C:/Users/16079/Code/PolicyLab && npx tsc --noEmit -p server/tsconfig.json 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - central-agent.ts buildAgentRosterMessages system prompt contains "5-8 sentence life story"
    - central-agent.ts buildAgentRosterMessages system prompt contains "economic philosophy"
    - central-agent.ts buildAgentRosterMessages system prompt contains "formative event"
    - central-agent.ts buildAgentRosterMessages system prompt does NOT contain "1-2 sentence background"
    - central-agent.ts buildAgentRosterMessages system prompt contains "economic instinct"
    - central-agent.ts buildResolutionPrompt still contains "Central Agent" (D-13 preserved)
    - central-agent.ts still contains "FRICTION" in NARRATIVE DIRECTIVE (D-14 preserved)
    - location.ts buildLocationAgentRosterMessages contains "5-8 sentence life story"
    - location.ts buildLocationAgentRosterMessages contains "economic instinct" or "economic philosophy"
    - location.ts buildLocationLawMessages is unchanged
    - location.ts buildScenarioInterpretationMessages is unchanged
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>Agent roster prompt updated to generate 5-8 sentence life stories with economic instincts per D-11/D-12 in both central-agent.ts and location.ts. Central Agent prompts preserved with mechanical framing per D-13. Narrative directive unchanged per D-14.</done>
</task>

<task type="auto">
  <name>Task 2: Align reflection prompts with immersion principles</name>
  <files>server/src/llm/prompts/reflection.ts</files>
  <read_first>
    server/src/llm/prompts/reflection.ts
  </read_first>
  <action>
    Per D-06/D-07, update citizen-facing reflection prompts to match the immersive tone established in Plan 02. These are prompts that speak TO citizens, not Central Agent system prompts.

    **reflection.ts changes:**

    1. `buildAgentReflectionPrompt` -- This prompts the citizen to reflect on their experiences.
       - Remove any "[SYSTEM" tags or "simulated society" framing
       - Change any "You are a citizen in a simulation" to first-person embodiment
       - Keep the reflection structure (what happened, how you feel, what you learned)
       - The agent should reflect as themselves, not as a player in a game

    2. `buildAgentReflection2Prompt` -- Second reflection pass.
       - Same immersion treatment as above

    3. `buildReviewChatPrompt` -- Post-simulation review chat with the user.
       - Remove simulation-awareness framing if present
       - The agent reviews their experience as lived experience

    4. `buildPostMortemPrompt` -- Post-mortem analysis.
       - This is analytical/meta so it may retain more structural framing
       - Remove any [SYSTEM TAG] labels but keep the analytical structure

    5. `buildEvaluationPrompt` -- This evaluates agent behavior.
       - This is a meta-evaluation prompt (not citizen-facing) so it KEEPS its analytical framing
       - Do NOT apply immersion changes to this prompt

    IMPORTANT: Do NOT modify any files other than reflection.ts in this task. location.ts was already handled in Task 1.
  </action>
  <verify>
    <automated>cd C:/Users/16079/Code/PolicyLab && npm run build 2>&1 | tail -5 && npm run test -w server -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - reflection.ts does NOT contain "[SYSTEM" tags in buildAgentReflectionPrompt or buildAgentReflection2Prompt
    - reflection.ts buildReviewChatPrompt does NOT contain "simulated" or "simulation"
    - reflection.ts buildEvaluationPrompt retains its analytical structure (this is NOT citizen-facing)
    - `npm run build` exits 0
    - `npm run test -w server -- --run` exits 0
  </acceptance_criteria>
  <done>Citizen-facing reflection prompts aligned with immersion principles (D-06/D-07). Analytical/meta prompts preserved. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Unskip and update prompt content tests for Plan 03 changes</name>
  <files>server/src/llm/__tests__/promptContent.test.ts</files>
  <read_first>
    server/src/llm/__tests__/promptContent.test.ts,
    server/src/llm/prompts/central-agent.ts
  </read_first>
  <action>
    Unskip the remaining test stubs from Plan 01 Task 3 that correspond to Plan 03 changes:

    1. **(D-11)** "rich life stories in roster" -- unskip, assert "5-8 sentence" present in buildAgentRosterMessages output
    2. **(D-13)** "Central Agent retains mechanical framing" -- unskip, assert "Central Agent" present in buildResolutionPrompt output

    Both tests should now pass against the updated prompt content.
  </action>
  <verify>
    <automated>cd C:/Users/16079/Code/PolicyLab && npx vitest run server/src/llm/__tests__/promptContent.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - D-11 and D-13 tests unskipped and passing
    - All 12 tests in promptContent.test.ts are active (0 skipped)
    - All tests pass with vitest
  </acceptance_criteria>
  <done>All prompt content tests unskipped and passing. Full coverage of D-02, D-04, D-05, D-06, D-07, D-08, D-09, D-11, D-13, D-17 verified.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0
2. `npm run test -w server -- --run` passes all tests
3. grep for "1-2 sentence background" in central-agent.ts and location.ts returns zero matches
4. grep for "5-8 sentence" in central-agent.ts and location.ts returns matches
5. grep for "Central Agent" in central-agent.ts buildResolutionPrompt returns match (D-13)
6. grep for "FRICTION" in central-agent.ts returns match (D-14)
7. All 12 prompt content tests pass (0 skipped)
</verification>

<success_criteria>
- Agent roster generates rich 5-8 sentence life stories with economic instincts (D-11/D-12) in both creative and location modes
- Central Agent prompts untouched -- mechanical framing preserved (D-13)
- Narrative directive friction-first bias preserved (D-14)
- Citizen-facing reflection prompts match immersive tone
- All prompt content tests unskipped and passing
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/09-redesign-prompts-for-all/09-03-SUMMARY.md`
</output>
