---
phase: 09-redesign-prompts-for-all
plan: 04
type: execute
wave: 3
depends_on: [09-02, 09-03]
files_modified:
  - server/src/orchestration/simulationRunner.ts
  - server/src/llm/__tests__/promptContent.test.ts
autonomous: true
requirements: [D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-11, D-13, D-17]

must_haves:
  truths:
    - "simulationRunner passes AMM spot price, food reserve, and fiat reserve to buildNaturalIntentPrompt"
    - "Prompt content tests verify accurate MET cost in output"
    - "Prompt content tests verify no mechanical [SYSTEM TAG] labels in citizen prompts"
    - "Prompt content tests verify Central Agent prompts retain mechanical framing"
    - "Prompt content tests verify natural language action descriptions"
    - "Prompt content tests verify rich life story format in roster prompt"
  artifacts:
    - path: "server/src/orchestration/simulationRunner.ts"
      provides: "AMM market data wiring to buildNaturalIntentPrompt"
      contains: "ammMarketData"
    - path: "server/src/llm/__tests__/promptContent.test.ts"
      provides: "Prompt content verification tests for D-04, D-06, D-07, D-13, D-17, D-11"
      min_lines: 80
  key_links:
    - from: "server/src/orchestration/simulationRunner.ts"
      to: "server/src/llm/prompts/agent-intent.ts"
      via: "ammMarketData parameter in buildNaturalIntentPrompt call"
      pattern: "ammMarketData"
    - from: "server/src/llm/__tests__/promptContent.test.ts"
      to: "server/src/llm/prompts/index.ts"
      via: "import and invoke prompt builders, assert content"
      pattern: "buildNaturalIntentPrompt"
---

<objective>
Wire AMM market data from simulationRunner into the rewritten intent prompt, and create prompt content tests that verify all critical D-* decisions are implemented correctly.

Purpose: The economic dashboard (D-02) needs live AMM spot prices passed from the simulation loop. Tests ensure the prompt rewrites survive future refactoring without regressing the death spiral fix or breaking immersion.

Output: Updated simulationRunner.ts with AMM data wiring + comprehensive prompt content test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-redesign-prompts-for-all/09-CONTEXT.md
@.planning/phases/09-redesign-prompts-for-all/09-RESEARCH.md
@.planning/phases/09-redesign-prompts-for-all/09-02-SUMMARY.md
@.planning/phases/09-redesign-prompts-for-all/09-03-SUMMARY.md
@server/src/orchestration/simulationRunner.ts (lines 1300-1450 for buildNaturalIntentPrompt call site)
@server/src/llm/prompts/agent-intent.ts
@server/src/llm/prompts/shared.ts
@server/src/llm/prompts/central-agent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AMM market data in simulationRunner.ts</name>
  <files>server/src/orchestration/simulationRunner.ts</files>
  <read_first>
    server/src/orchestration/simulationRunner.ts
  </read_first>
  <action>
    Find the call site where `buildNaturalIntentPrompt` is invoked in simulationRunner.ts. This is in the intent phase where each agent's LLM call is prepared.

    Before the call, compute AMM market data from the session's AMM registry:

    ```typescript
    const amm = sessionAMMRegistry.get(sessionId);
    const ammMarketData = amm ? {
      foodSpotPrice: amm.spotPrice('food'),
      foodReserve: amm.getReserve('food'),
      fiatReserve: amm.getFiatReserve(),
    } : undefined;
    ```

    NOTE: The exact AMM API methods may differ. Read the AMM class/interface to find the correct method names for:
    1. Getting the spot price for food
    2. Getting the food goods reserve amount
    3. Getting the fiat reserve amount

    The AMM is a constant-product AMM (`automatedMarketMaker.ts`). Check the class for `spotPrice()`, `getSpotPrice()`, or similar methods. Also check if the AMM tracks per-commodity reserves or a single reserve pair.

    Pass `ammMarketData` as the new parameter to `buildNaturalIntentPrompt`. The parameter was added in Plan 02 after `centralBankContext` and before `subconsciousDrive`.

    The AMM data should be computed ONCE per iteration (outside the per-agent loop) since it's the same for all agents in the same iteration. Then pass it to each agent's prompt call.

    If the AMM uses a single food/fiat pair, the data extraction is straightforward. If it uses multiple commodity pairs, extract only the food pair.
  </action>
  <verify>
    <automated>cd C:/Users/16079/Code/PolicyLab && npx tsc --noEmit -p server/tsconfig.json 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - simulationRunner.ts contains `ammMarketData` variable assignment
    - simulationRunner.ts passes `ammMarketData` to `buildNaturalIntentPrompt`
    - The AMM data is computed from `sessionAMMRegistry` (or equivalent AMM access pattern)
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>simulationRunner.ts passes live AMM market data (food spot price, food reserve, fiat reserve) to buildNaturalIntentPrompt. Agents will now see real market prices in their economic dashboard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create prompt content verification tests</name>
  <files>server/src/llm/__tests__/promptContent.test.ts</files>
  <behavior>
    - Test 1 (D-04): buildNaturalIntentPrompt output contains "5-6" food cost and does NOT contain "1 Food is consumed automatically"
    - Test 2 (D-06): buildNaturalIntentPrompt output does NOT contain "simulated society" or "simulated"
    - Test 3 (D-07): buildNaturalIntentPrompt output does NOT contain "[BIOLOGICAL SUBCONSCIOUS]", "[CAPITALIST IDENTITY]", "[LEGAL RISK ASSESSMENT]", "[AVAILABLE ACTIONS]", "[BACKGROUND SYSTEM]", "[MARKET KNOWLEDGE]"
    - Test 4 (D-08): buildNaturalIntentPrompt output with health < 20 contains "cold" and "darkness" (biological prose preserved)
    - Test 5 (D-17): buildActionDictionary output contains "(PRODUCE_AND_SELL)" (natural language + code format)
    - Test 6 (D-17): buildActionDictionary output does NOT contain "[AVAILABLE ACTIONS]"
    - Test 7 (D-13): buildResolutionPrompt output contains "Central Agent" (mechanical framing preserved)
    - Test 8 (D-11): buildAgentRosterMessages output contains "5-8 sentence" (rich life stories)
    - Test 9 (D-02): buildNaturalIntentPrompt with ammMarketData contains "fiat per unit" (market data shown)
    - Test 10 (D-05): buildNaturalIntentPrompt with isFirstIteration=true contains "fair price" or "3-5 fiat"
  </behavior>
  <read_first>
    server/src/llm/prompts/agent-intent.ts,
    server/src/llm/prompts/shared.ts,
    server/src/llm/prompts/central-agent.ts,
    server/src/__tests__/sfcInflation.test.ts (for import pattern and test agent/session fixtures)
  </read_first>
  <action>
    Create `server/src/llm/__tests__/promptContent.test.ts` with vitest tests.

    Import `buildNaturalIntentPrompt`, `buildActionDictionary`, `buildResolutionPrompt`, `buildAgentRosterMessages` from `../prompts/index.js`.

    Create minimal test fixtures:
    ```typescript
    const testAgent = {
      id: 'test-agent-1',
      name: 'Tomas',
      role: 'farmer',
      background: 'A simple farmer from the eastern ridge.',
      personalityTraits: ['risk-averse'],
      currentStats: { wealth: 50, health: 70, happiness: 60, cortisol: 20, dopamine: 50 },
      type: 'citizen',
    } as any;

    const testSession = {
      idea: 'A medieval farming village',
      societyOverview: 'A small village with farmers and merchants.',
      law: 'Basic property rights are enforced.',
      timeScale: '1 iteration = 1 week',
    };
    ```

    Write 10 tests as described in the behavior section. Each test calls the relevant prompt builder with minimal args and asserts content presence/absence using `expect(output).toContain()` and `expect(output).not.toContain()`.

    For `buildNaturalIntentPrompt`, extract the text content from the returned `LLMMessage[]` array. The system message has `content` as either a string or `ContentBlock[]` array. Concatenate all text blocks into a single string for assertion.

    Helper to extract all text from messages:
    ```typescript
    function extractText(messages: any[]): string {
      return messages.map(m => {
        if (typeof m.content === 'string') return m.content;
        if (Array.isArray(m.content)) return m.content.map((b: any) => b.text || '').join('\n');
        return '';
      }).join('\n');
    }
    ```
  </action>
  <verify>
    <automated>cd C:/Users/16079/Code/PolicyLab && npx vitest run server/src/llm/__tests__/promptContent.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - server/src/llm/__tests__/promptContent.test.ts exists
    - File contains `import { buildNaturalIntentPrompt` from the prompts barrel
    - File contains at least 10 test cases (it/test blocks)
    - Test for D-04 asserts "5-6" present and "1 Food is consumed" absent
    - Test for D-07 asserts "[BIOLOGICAL SUBCONSCIOUS]" absent
    - Test for D-13 asserts "Central Agent" present in resolution prompt
    - Test for D-17 asserts "(PRODUCE_AND_SELL)" present in action dictionary
    - Test for D-11 asserts "5-8 sentence" present in roster prompt
    - All 10 tests pass when run with vitest
    - `npm run test -w server -- --run` passes all tests (including new + existing)
  </acceptance_criteria>
  <done>10 prompt content tests created and passing. Tests verify: accurate MET cost (D-04), no simulation framing (D-06), no system tags (D-07), biological prose preserved (D-08), natural action format (D-17), Central Agent mechanical framing (D-13), rich life stories (D-11), market data display (D-02), iteration 1 anchoring (D-05).</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0
2. `npm run test -w server -- --run` passes all tests (195+ existing + 10 new)
3. `npx vitest run server/src/llm/__tests__/promptContent.test.ts` — all 10 tests pass
4. grep for "ammMarketData" in simulationRunner.ts returns match
5. grep for "ammMarketData" in agent-intent.ts returns match (parameter exists)
</verification>

<success_criteria>
- Live AMM market data flows from simulation loop to citizen intent prompt
- 10 prompt content tests verify all critical D-* decisions are implemented
- All existing + new tests pass
- The death spiral fix, immersion changes, and roster enrichment are regression-protected
</success_criteria>

<output>
After completion, create `.planning/phases/09-redesign-prompts-for-all/09-04-SUMMARY.md`
</output>
