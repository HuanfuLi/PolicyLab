---
phase: 04-inflation-loop
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - server/src/mechanics/actionCodes.ts
  - server/src/mechanics/skillSystem.ts
  - server/src/mechanics/physicsEngine.ts
autonomous: true
requirements: [BANK-07]

must_haves:
  truths:
    - "SET_RESERVE_RATIO and SET_BASE_RATE are valid ActionCodes in the union type"
    - "Only central_bank role agents can use SET_RESERVE_RATIO and SET_BASE_RATE"
    - "Physics engine clamps SET_RESERVE_RATIO value to [0.05, 0.50]"
    - "Physics engine clamps SET_BASE_RATE value to [0.001, 0.05]"
    - "Resolved SET_RESERVE_RATIO updates session economyConfig.reserveRequirement"
    - "Resolved SET_BASE_RATE updates session economyConfig.baseLoanInterestRate"
  artifacts:
    - path: "server/src/mechanics/actionCodes.ts"
      provides: "SET_RESERVE_RATIO and SET_BASE_RATE action codes with central_bank role gating"
      contains: "SET_RESERVE_RATIO"
    - path: "server/src/mechanics/skillSystem.ts"
      provides: "ACTION_SKILL_MAP entries for new action codes"
      contains: "SET_RESERVE_RATIO"
    - path: "server/src/mechanics/physicsEngine.ts"
      provides: "Resolution cases for central bank actions with value clamping"
      contains: "SET_RESERVE_RATIO"
  key_links:
    - from: "server/src/mechanics/physicsEngine.ts"
      to: "server/src/mechanics/actionCodes.ts"
      via: "ActionCode type import and switch case"
      pattern: "SET_RESERVE_RATIO.*SET_BASE_RATE"
    - from: "server/src/mechanics/actionCodes.ts"
      to: "shared/src/types.ts"
      via: "ActionCode union type"
      pattern: "SET_RESERVE_RATIO"
---

<objective>
Register the central bank agent's two action codes (SET_RESERVE_RATIO, SET_BASE_RATE) and implement their physics engine resolution cases with value clamping and EconomyConfig update.

Purpose: The central bank agent needs mechanical levers to adjust monetary policy in response to inflation data. These action codes allow the LLM-driven central bank agent to tighten or loosen policy, directly affecting the banking engine's behavior on subsequent iterations.

Output: Two new ActionCodes registered in `actionCodes.ts` with central_bank role gating, exhaustiveness entries in `skillSystem.ts`, and resolution cases in `physicsEngine.ts` that clamp values and update session config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-inflation-loop/04-RESEARCH.md
@.planning/phases/04-inflation-loop/04-01-SUMMARY.md
@server/src/mechanics/actionCodes.ts
@server/src/mechanics/skillSystem.ts
@server/src/mechanics/physicsEngine.ts
@shared/src/types.ts

<interfaces>
<!-- From 04-RESEARCH.md Pattern 5: Central Bank Agent -->
Physics engine validation ranges:
- reserveRequirement: clamp to [0.05, 0.50]
- baseLoanInterestRate: clamp to [0.001, 0.05] per-iteration

EconomyConfig update pattern (from ADJUST_TAX precedent):
```typescript
const sessionConfig = JSON.parse(session.config ?? '{}');
const econConf = getEconomyConfig(sessionConfig);
econConf.reserveRequirement = clamp(newValue, 0.05, 0.50);
sessionConfig.economyConfig = econConf;
// persist via sessionRepo.updateConfig(sessionId, JSON.stringify(sessionConfig))
```

From server/src/mechanics/actionCodes.ts:
```typescript
export type ActionCode = 'WORK' | 'REST' | ... | 'ISSUE_GOV_BOND' | 'NONE';
const VALID_ACTIONS: Set<string> = new Set([...]);
const BANK_ACTIONS: readonly ActionCode[] = ['DEPOSIT', 'WITHDRAW', 'TAKE_LOAN', ...];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register SET_RESERVE_RATIO and SET_BASE_RATE action codes</name>
  <files>server/src/mechanics/actionCodes.ts, server/src/mechanics/skillSystem.ts, shared/src/types.ts</files>
  <read_first>server/src/mechanics/actionCodes.ts, server/src/mechanics/skillSystem.ts, shared/src/types.ts</read_first>
  <action>
1. In `shared/src/types.ts`, if ActionCode is re-exported or referenced there, ensure the union type includes the two new codes. (Check if ActionCode lives in types.ts or only in actionCodes.ts — follow whichever is the source of truth.)

2. In `server/src/mechanics/actionCodes.ts`:
   - Add `| 'SET_RESERVE_RATIO'` and `| 'SET_BASE_RATE'` to the `ActionCode` union type, in a new comment block `// Inflation Loop (Phase 4: Central Bank)`
   - Add both to the `VALID_ACTIONS` set
   - Create a new `CENTRAL_BANK_ACTIONS: readonly ActionCode[]` array containing `['SET_RESERVE_RATIO', 'SET_BASE_RATE']`
   - In `getAllowedActions()`, add a case for `'central_bank'` role that returns `[...BASE_ACTIONS, ...BANK_ACTIONS, ...CENTRAL_BANK_ACTIONS]` (the central bank can also do basic banking operations). Follow the existing pattern for how `bank` role is handled.

3. In `server/src/mechanics/skillSystem.ts`:
   - Add entries to `ACTION_SKILL_MAP` for the new action codes (TypeScript Record exhaustiveness requires this):
     ```
     SET_RESERVE_RATIO: { primary: 'management', secondary: null },
     SET_BASE_RATE: { primary: 'management', secondary: null },
     ```
   - This prevents the TypeScript compiler error from the exhaustive Record<ActionCode, ...> type.
  </action>
  <verify>
    <automated>cd C:/Users/16079/Code/PolicyLab && npx tsc --noEmit -p server/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep "SET_RESERVE_RATIO" server/src/mechanics/actionCodes.ts returns the union member
    - grep "SET_BASE_RATE" server/src/mechanics/actionCodes.ts returns the union member
    - grep "CENTRAL_BANK_ACTIONS" server/src/mechanics/actionCodes.ts returns the array
    - grep "central_bank" server/src/mechanics/actionCodes.ts returns the role case in getAllowedActions
    - grep "SET_RESERVE_RATIO" server/src/mechanics/skillSystem.ts returns the skill map entry
    - TypeScript compiles cleanly (no exhaustiveness errors)
  </acceptance_criteria>
  <done>SET_RESERVE_RATIO and SET_BASE_RATE registered in ActionCode union, VALID_ACTIONS set, CENTRAL_BANK_ACTIONS array, getAllowedActions for central_bank role, and ACTION_SKILL_MAP. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Physics engine resolution cases for central bank actions</name>
  <files>server/src/mechanics/physicsEngine.ts</files>
  <read_first>server/src/mechanics/physicsEngine.ts, server/src/orchestration/simulationRunner.ts</read_first>
  <action>
1. In `server/src/mechanics/physicsEngine.ts`, find the action resolution switch/if-chain (where ADJUST_TAX, DEPOSIT, TAKE_LOAN etc. are handled). Add two new resolution cases:

   **SET_RESERVE_RATIO:**
   - Extract the `value` parameter from the action (same pattern as ADJUST_TAX extracts its value)
   - Clamp to [0.05, 0.50]: `const clamped = Math.max(0.05, Math.min(0.50, value))`
   - If the agent role is not `central_bank`, reject the action (trace: "Only central_bank agents can set reserve ratio")
   - Return a delta/trace that indicates the new reserve ratio. The actual EconomyConfig update will be handled by the simulation runner (following the same pattern as ADJUST_TAX updates session.config.policy.tax_rate)
   - Add to trace: `"Central bank set reserve ratio to ${clamped} (requested ${value})"`

   **SET_BASE_RATE:**
   - Extract the `value` parameter from the action
   - Clamp to [0.001, 0.05]: `const clamped = Math.max(0.001, Math.min(0.05, value))`
   - Same role check as above
   - Return delta/trace with the new base rate
   - Add to trace: `"Central bank set base rate to ${clamped} (requested ${value})"`

2. Follow the EXACT same resolution pattern used by ADJUST_TAX for how the resolved value is communicated back to the simulation runner. Look at how ADJUST_TAX returns its result — the central bank actions should use the same mechanism (likely a field on the resolution result object or a special delta type).

3. Ensure the physics engine does NOT directly write to DB or mutate session config. It returns the clamped value in the resolution result; the simulation runner (Plan 03) will apply it to EconomyConfig.
  </action>
  <verify>
    <automated>cd C:/Users/16079/Code/PolicyLab && npx tsc --noEmit -p server/tsconfig.json && npm run test -w server 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "SET_RESERVE_RATIO" server/src/mechanics/physicsEngine.ts returns the resolution case
    - grep "SET_BASE_RATE" server/src/mechanics/physicsEngine.ts returns the resolution case
    - grep "0.05.*0.50" server/src/mechanics/physicsEngine.ts shows reserve ratio clamping
    - grep "0.001.*0.05" server/src/mechanics/physicsEngine.ts shows base rate clamping
    - grep "central_bank" server/src/mechanics/physicsEngine.ts shows role validation
    - TypeScript compiles; existing tests pass (no regressions)
  </acceptance_criteria>
  <done>Physics engine resolves SET_RESERVE_RATIO and SET_BASE_RATE with value clamping and central_bank role validation. No DB mutations in physics engine. Existing tests unbroken.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p server/tsconfig.json` passes
- `npm run test -w server` all existing tests pass (no regressions)
- `grep "SET_RESERVE_RATIO" server/src/mechanics/actionCodes.ts server/src/mechanics/physicsEngine.ts server/src/mechanics/skillSystem.ts` returns matches in all three files
- `grep "CENTRAL_BANK_ACTIONS" server/src/mechanics/actionCodes.ts` returns the array
</verification>

<success_criteria>
- SET_RESERVE_RATIO and SET_BASE_RATE are valid ActionCodes with CENTRAL_BANK_ACTIONS gating
- Physics engine clamps reserve ratio to [0.05, 0.50] and base rate to [0.001, 0.05]
- Only central_bank role can execute these actions
- Resolution follows same pattern as ADJUST_TAX (returns delta, no DB mutation)
- All existing tests pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/04-inflation-loop/04-02-SUMMARY.md`
</output>
